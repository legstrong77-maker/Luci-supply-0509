name: Deploy to GitHub Pages

# 部署前置作業（一次性）：
#   Repo → Settings → Pages → Source 選 "GitHub Actions"
# 完成後，本 workflow 會在 push 到 main 時自動部署，
# 網址會出現在 Actions 的 deployment summary。

on:
  push:
    branches: [main, "claude/hiking-club-website-YB5qy"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true   # 自動啟用 GitHub Pages（不用手動到 Settings 開）

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
